# library-management-api-fastapi
A RESTful Library Management API built with FastAPI and Pydantic featuring CRUD operations, Field validation, response models, HTTPException handling, and partial updates using PATCH.
